# JOY Badge Assets

SVG assets are pending restoration.

Existing receipts preserve claimed hashes from prior local builds, but each SVG must be restored and verified with `sha256sum` before being marked canonical.

## Rule

SVG → local SHA-256 → receipt update → manifest update → ledger update → GitHub commit.
