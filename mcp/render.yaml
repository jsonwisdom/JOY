services:
  - type: web
    name: alms-mcp-v0
    runtime: python
    buildCommand: pip install -r mcp/requirements.txt
    startCommand: uvicorn mcp.alms_mcp_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
