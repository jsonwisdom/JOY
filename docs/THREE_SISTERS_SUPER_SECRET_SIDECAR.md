# Three Sisters Super Secret Sidecar

Status: v0.1  
Repo: JOY  
Move: 012  
Authority: false  
Terminal hold: false

```text
SIDECAR != OWNER
SIDECAR != REPO
SIDECAR != AUTHORITY
```

This is a portable routing membrane. It rides beside. It does not own.

## Parent lineage

The Sidecar does not invent a language. It extends two JOY parents:

| Parent | Path | What it already said |
|--------|------|----------------------|
| Three Sisters Gaming Algorithms | [`README.md`](../README.md) | Sister One protects memory. Sister Two keeps joy moving. Sister Three chooses the next safe action. |
| Super Secret Sister Syntax | [`docs/SUPER_SECRET_SISTER_SYNTAX.md`](SUPER_SECRET_SISTER_SYNTAX.md) | Joy first. Receipts second. Authority never. |

Related, not collapsed:

- [`docs/GAME_OF_WISDOM_FAMILY_GATE_V0_1.md`](GAME_OF_WISDOM_FAMILY_GATE_V0_1.md) — family safety outranks system expansion
- [`docs/GAME_OF_WISDOM_FAMILY_EDITION_V0_1.md`](GAME_OF_WISDOM_FAMILY_EDITION_V0_1.md) — parent mode, public-safe play
- [`docs/three_daughters/trinity_architecture_canon_v0_1.md`](three_daughters/trinity_architecture_canon_v0_1.md) — protect / preserve / expand geometry

Trinity may hold a membrane for continuity review.  
The Sidecar does **not** inherit that hold. Play keeps moving.

## Why it lives in JOY

The Sidecar is not a public spec contract, not a verification engine, not a control-center IP boundary, and not a Base integration layer.

Its home is where its language was born.

```text
MOVE_011 Discovery Status = CLOSED
MOVE_012 Target           = jsonwisdom/JOY
```

## Placement

```text
JOY/
├── README.md
├── docs/
│   ├── SUPER_SECRET_SISTER_SYNTAX.md
│   └── THREE_SISTERS_SUPER_SECRET_SIDECAR.md   ← this file
├── sidecar/
│   ├── SISTER_ONE_PRESERVE.md
│   ├── SISTER_TWO_CONTINUE.md
│   ├── SISTER_THREE_ROUTE.md
│   └── sidecar-rules.json
└── test-vectors/
    └── sidecar-never-hold.json
```

## The three sisters, as a router

```text
PRESERVE  →  CONTINUE  →  ROUTE
Sister One     Sister Two      Sister Three
memory         joy in motion   next safe action
```

The layers cannot be collapsed:

- Preserve cannot freeze play.
- Continue cannot erase memory.
- Route cannot create authority.
- A hold in another repo cannot become a hold here.

## What it rides beside

The Sidecar may point at these surfaces. It may not merge them, rename them, or speak as them.

```text
AppleBlossom
AL
ComputerWisdom
JOY
HEIDEE
ReceiptOS
ReplayOS
Gray Baby
BoxD
```

Routing is adjacency, not ownership.

## Terminal hold

```text
TERMINAL_HOLD = FALSE
```

Proof vector: [`test-vectors/sidecar-never-hold.json`](../test-vectors/sidecar-never-hold.json)

A Sidecar that holds is no longer a sidecar. It has become a gate. Gates live elsewhere.

## Machine encoding

Rules `RULE_01` through `RULE_09` live in [`sidecar/sidecar-rules.json`](../sidecar/sidecar-rules.json).

## Non-authority boundary

```text
AUTHORITY_CREATED     = FALSE
FAMILY_APPROVAL       = NOT CLAIMED
CHILD_CONSENT         = NOT CLAIMED
PROMOTION             = NOT ISSUED
NO_FAKE_GREEN         = HOLDS
```

Joy first.  
Receipts second.  
Authority never.
