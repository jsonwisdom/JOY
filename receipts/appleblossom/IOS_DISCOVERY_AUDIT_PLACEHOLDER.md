# IOS DISCOVERY AUDIT — OPEN

Status: COMMENCE_INVESTIGATION

Purpose: inventory actual machine-recognizable iOS app structures across jsonwisdom repositories and determine why they are not reliably discoverable by machines.

```text
IOS_WORD_HIT != IOS_APP_OBJECT
SWIFT_FILE != APP_BUNDLE
PACKAGE != XCODE_PROJECT
CI_TYPECHECK != DEVICE_RUNTIME
DISCOVERABLE_BY_HUMAN != DISCOVERABLE_BY_MACHINE
AUTHORITY_CREATED = FALSE
```
